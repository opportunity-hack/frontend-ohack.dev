# Appendices — detailed executor notes per workstream

Companion to `team-dashboard-devpost-replacement.md`. These are the detailed designs produced during planning (2026-09-19), edited for the reconciled contracts. **If anything here conflicts with Parts 3–4 of the main plan, the main plan wins.** Cancelled during review and removed from these appendices: any change to the judging process (rubric consolidation, judge scoring-page redesign, rubric copy edits, `mentorCoverage` label rewording, `JudgingRound1` completeness logic) and the signed-URL image mint (the existing `POST /api/messages/upload-image` is used instead).

---

## Appendix A — Backend (WS-A) — `backend-ohack.dev`, branch `feat/submissions-peer-vote`

Python 3.9 (`Optional[X]`, never `X | None`). Per-directory tests: `ENVIRONMENT=test pytest api/<domain>/tests`. Admin routes use `@auth.require_org_member_with_permission("volunteer.admin", req_to_org_id=getOrgId)`; view params == URL params; identity from `auth_user.user_id`. Admin bypass inside services: `is_admin(propel_user)` from `services/hackathon_planning_service.py` L28, passed in as `admin: bool`.

### A0. Data contracts
See Part 3 of the main plan (authoritative). Additional writer notes:
- `project_submission_status` is set to `draft` on the first project save; `submit` sets `submitted` or `late` (window state `late`), never back to draft by hackers; admins may override via `PATCH /api/team/edit` (add the `project_*` keys + `project_submission_status` to `edit_team`'s `field_mappings`, `api/teams/teams_service.py` L282–297).
- `mentor_help_wanted` absent ⇒ True (document in README + CLAUDE.md).
- No composite indexes: every query is single-field or multi-equality with Python sort/filter.

### A1. Hackathon `deadlines` + peer-vote constraints
`common/utils/validators.py`: constants `DEADLINE_KEYS`, `PEER_VOTE_SLATE_SIZE_RANGE = (3, 10)`, `PEER_VOTE_MAX_PICKS_RANGE = (1, 5)`; `normalize_deadline_iso(value, tz_name)` (trailing `Z` → `+00:00`; naive → `ZoneInfo(tz_name)`; returns `.isoformat()`); `validate_deadlines(deadlines, tz_name)` (unknown key → `ValueError`; `None`/`""` ⇒ `None` = explicit clear; ordering `submission <= late_submission_until`, `voting_opens < voting_closes`); `sanitize_markdown(text, max_length)`; `validate_https_url(url, max_length=2048)`. Wire into `validate_hackathon_data_partial` after the `github_org` block (L350–354) with `_skip` on failure, and validate `peer_vote_enabled` (bool), `peer_vote_slate_size`, `peer_vote_max_picks` (must be `< slate_size`), `peer_vote_requires_submission` inside the constraints block before `cleaned["constraints"] = c` (L312).
`services/hackathons_service.py::save_hackathon` after L1216–1218:
```python
if "deadlines" in data:
    dl = data["deadlines"]
    hackathon_data["deadlines"] = ({k: (firestore.DELETE_FIELD if v is None else v) for k, v in dl.items()}
                                   if is_update else {k: v for k, v in dl.items() if v is not None})
```
`get_single_hackathon_event` (L598–600): after `_enrich_teams_users_batch`, `for t in result["teams"]: t.pop("project_story", None)`.
Tests: `test/common/utils/test_validators.py` (+naive→offset, `Z`, ordering, unknown key, ranges); new `api/messages/tests/test_hackathon_deadlines.py` (mock `_get_db` like `test_cache_invalidation.py`; update writes DELETE_FIELD for None; create drops None).

### A2. `api/submissions/` blueprint
Files: `__init__.py`, `submissions_service.py`, `submissions_views.py` (`bp = Blueprint("api-submissions", __name__, url_prefix="/api")`), `tests/__init__.py`, `tests/test_submissions_service.py`, `tests/test_submissions_views.py`, `README.md`. Lazy-import heavy deps inside functions (surveys style).
```python
PROJECT_LIMITS = {"tagline":140,"story":20000,"built_with_n":25,"built_with_len":30,"links_n":10,"label":40,"url":2048,"images_n":8}
PROJECT_FIELDS = ("project_tagline","project_story","project_built_with","project_links","project_thumbnail_url","project_images")
SUBMITTED_STATUSES = {"submitted","late"}
def clear_cache() -> None                    # copy api/mentors/mentors_service.py L33–49 (clear_all_caches + lazy hackathons_service.clear_cache)
def _notifications_disabled() -> bool        # ENVIRONMENT == "test"
def compute_submission_window(event, now=None) -> dict   # {"state": open|late|closed|no_deadline, "submission", "late_until", "now", "timezone"}
def submissions_closed(window) -> bool
def _team_or_404(team_id)                    # mirror mentors _team_doc_or_404
def _authorize_team_write(propel_user_id, team_id, admin, enforce_deadline=True)  # -> (error|None, team, event, window); 404 / 403 {"error":"not_team_member"} / 409 {"error":"submissions_closed","deadline","late_until","now"}
def validate_project_payload(payload, team_id) -> (clean, errors[{field, reason}])
def save_project(propel_user_id, team_id, payload, admin=False) -> (dict, int)
def submit_project(propel_user_id, team_id, admin=False) -> (dict, int)
def self_serve_team_edit(propel_user_id, team_id, fields, admin=False) -> (dict, int)   # A3
def set_mentor_help_wanted(propel_user_id, team_id, open_flag, admin=False) -> (dict, int)  # A4
def get_submission_window_for_event(event_id) -> (dict, int)
```
Rules: `save_project` is a partial update (only present `PROJECT_FIELDS` keys; `set(merge=True)` is safe — arrays replace wholesale); stamps `project_updated_at`; sets `draft` if status absent; editing stays allowed after submit until the window closes; returns `{"success": True, "team": get_team(team_id)["team"], "window": window}`; validation errors → 400 `{"error":"invalid_project","errors":[...]}`. `submit_project`: 400 `{"error":"incomplete","missing":[...]}` without tagline+story; already submitted → 200 `{success, already_submitted: true, team}`; else stamp `project_submitted_at`, status `late` if window is `late` (admin bypass on closed also records `late`); Slack to `team.slack_channel` (gated by `_notifications_disabled`) + `send_slack_audit(action="project_submit")`; `clear_cache()`.
**Sanitization decision (documented in README + docstring):** raw markdown stored; the frontend renders with react-markdown without `rehype-raw`, so raw HTML is inert there. `sanitize_markdown` is defence-in-depth: strip control chars (keep `\n\t`), remove tags named `script|iframe|object|embed|style|link|meta|form|base` (open/close), strip `on\w+=` attributes inside any `<...>`, rewrite `javascript:|vbscript:|data:` link/image targets to `#`. Do NOT strip generic `<` — code like `List<String>` must survive. Apply to tagline and story; NFC-normalize; truncate after stripping.
`project_links[].url` → `validate_https_url`; `project_built_with` entries length-checked; `project_thumbnail_url` / `project_images[]` must start with `f"{CDN_SERVER}/teams/{team_id}/"` (`CDN_SERVER = os.getenv("CDN_SERVER","https://cdn.ohack.dev")`, pattern `services/users_service.py` L1092–1140); for URLs not already on the doc, verify with `common.utils.cdn.get_blob_metadata` (image type, ≤5MB) → else 400 `upload_not_found`.
Views (`@auth.require_user`; `admin = is_admin(auth_user)`): `POST /api/team/<teamid>/project`, `POST /api/team/<teamid>/project/submit`, `POST /api/team/<teamid>/mentor-availability`, and **public** `GET /api/hackathons/<event_id>/submissions/window` (404 if event missing).
Tests (MockFirestore via `patch.object(svc, "get_db")`; patch `svc.user_is_on_team`, `svc.get_team`, `svc.get_hackathon_by_event_id`, `svc.get_blob_metadata`): window open/late/closed/no_deadline with tz + injected `now`; sanitizer keeps `List<String>`, strips `<script>`, `onerror=`, `javascript:`; payload limits (141-char tagline, 26 tags, 11 links, http URL, foreign-CDN URL); non-member 403; closed 409 with `deadline`; admin bypass; first save sets draft; submit → submitted vs late; resubmit idempotent; edit-after-submit keeps status. Views: copy the fixture from `api/volunteers/tests/test_volunteers_views.py` L1–90 + the signature test.

### A3. Auth-gap fix on `/devpost` + `/demo-video`
`self_serve_team_edit`: `_authorize_team_write` (403/409) then `edit_team({"id": team_id, **fields})` (keeps `*_submitted` stamping) → `{**edit_result, "team": get_team(team_id)["team"]}`. `api/teams/teams_views.py` L77–114: keep the 400 checks, replace `return edit_team(...)` with `return self_serve_team_edit(auth_user.user_id, teamid, {...}, admin=is_admin(auth_user))` (lazy import inside the view; `teams_service` must never import submissions).

### A4. Mentor availability
`set_mentor_help_wanted`: non-bool → 400; `_authorize_team_write(..., enforce_deadline=False)`; write `{mentor_help_wanted, mentor_help_wanted_updated_at, mentor_help_wanted_updated_by_name}` (name via `get_propel_user_details_by_id(...)[4]`); `send_slack_audit(action="mentor_help_wanted")` only; `clear_cache()`; return `{"success": True, "team": get_team(...)["team"]}`.

### A5. Self-check widening
`services/volunteers_service.py::get_volunteer_self_status(propel_user_id, event_id, volunteer_type)` next to `find_volunteer_by_caller_identity` (L121) → `{f"is_{type}": bool(doc and doc.get("isSelected")), "volunteer": {"name", "isSelected"} | None}`. `api/volunteers/volunteers_views.py` L238–243: `mentor` → existing; `hacker` → new; else 400. Tests in `api/volunteers/tests/test_volunteers_service.py`.

### A6. GitHub activity proxy
`common/utils/github.py::get_repo_activity(org_name, repo_name)`: `g = Github(os.getenv("GITHUB_TOKEN"), per_page=100)`; exactly 3 calls — `repo = g.get_repo(f"{org}/{repo}")`, `commits = list(repo.get_commits().get_page(0))` (catch `GithubException` 409 "empty repository" → `[]`), `open_prs = repo.get_pulls(state="open").totalCount`. Contributors derived from the commit page (`Counter` of `c.author.login` + `avatar_url`; fallback `c.commit.author.name`), top 8. Returns the Part 3 shape; raises `UnknownObjectException` / `RateLimitExceededException` to the caller.
`api/github/github_service.py::get_github_activity(org, repo)`: validate both against `^[A-Za-z0-9_.-]{1,100}$` (400 `invalid_repo`); `_ACTIVITY_CACHE = TTLCache(512, 300)` success-only; 404 → `{"error":"repo_not_found"}` (uncached); rate limit → 503 `{"error":"github_rate_limited","reset_at"}` (uncached); other → 502 `github_unavailable`. View `GET /api/github/activity?org&repo` public, mirrors `/issues` (L176–205) but `org` required. While here (Part 9 #7): make `/issues` 400 when `org` is missing and fix the `len(issues)` log.
Tests `api/github/tests/test_github_activity.py`: patch `common.utils.github.Github` with a fake; assert exactly 3 calls, `last_24h` with injected `now`, top-8 contributors, empty-repo → zeros; service caches success only.

### A7. `api/peer_votes/` blueprint
```python
PEER_VOTES_COLLECTION = "peer_votes"; AWARD_NAME = "Hackers' Choice"; DEFAULT_SLATE_SIZE = 5; DEFAULT_MAX_PICKS = 2
def _ballot_doc_id(event_id, propel_id)          # f"{event_id}__{propel_id.replace('/','_')}"
def _peer_vote_subdoc(db, hackathon_doc_id, name)
def _settings(event) -> dict                      # enabled (constraints.peer_vote_enabled, default False), slate_size, max_picks, requires_submission
def compute_voting_window(event, now=None)        # {"state": upcoming|open|closed, "opens_at", "closes_at"}; opens default = late_submission_until or submission; closes default = end_date 23:59:59 event tz; no deadlines → closed
def _voter_eligibility(propel_id, event_id, event) -> (bool, volunteer|None, reason)   # find_volunteer_by_caller_identity(..., "hacker") + isSelected; requires_submission check
def _submitted_teams_for_event(event_id)          # teams.where("hackathon_event_id","==",event_id) → python filter active != False and status in SUBMITTED
def _own_team_ids(propel_id, event_id) -> set     # via get_my_teams_by_event_id
def build_slate(candidates, exposure, event_id, propel_id, n)   # rng = random.Random(int(sha256(f"{event_id}:{propel_id}").hexdigest(),16)); shuffle copy; stable sort by exposure.get(id,0); take n
def _in_transaction(db, body)                     # tests monkeypatch to body(FakeTx)
def _slate_team_view(team)                        # {team_id, name, project_tagline, project_thumbnail_url, demo_video_url, github_links, users_count}
def get_slate(propel_id, event_id) / submit_ballot(propel_id, event_id, picks)
def wilson_lower_bound(approvals, shown, z=1.96) -> float
def compute_results(ballots, teams_by_id, exposure) -> list   # pure; rank by wilson desc, approvals desc, name
def get_results(event_id) / void_ballot(event_id, voter_propel_id, actor) / publish_results(event_id, actor, team_id=None) / get_public_summary(event_id)
```
`get_slate`: event 404 → `enabled` false ⇒ `{status:"disabled"}` → window → eligibility → existing ballot doc? return persisted (`status = "voted" if picks else window.state`) → candidates minus own teams; `< 2` ⇒ `{status:"open", slate:[], reason:"not_enough_submissions"}` without persisting; else `build_slate` and persist in `_in_transaction` (re-`get` first; `tx.set(ballot_ref, {..., picks: None})`; `tx.set(exposure_ref, {"counts": {tid: firestore.Increment(1)}}, merge=True)`). `submit_ballot`: 403 `peer_vote_disabled`/`not_eligible`; 409 `voting_closed`; 400 `no_slate`; picks = unique list 1..max_picks all in slate else 400 `invalid_picks`; 409 `ballot_voided`; full-doc write (`voted_at` first vote only); re-vote allowed until close; audit only. `get_results`: non-voided ballots with picks → per team `shown/approvals/approval_rate/wilson/rank`, `exposure_shown`, `ballots`, `voided`, `eligible_estimate` (count of `isSelected` in `get_all_hackers_by_event_id`), `window`, `settings`, `published`. `void_ballot`: merge-set `{voided: True, voided_at, voided_by}` (top-level scalars). `publish_results`: winner = arg or rank 1 (409 `no_ballots`); append `AWARD_NAME` to `teams/{id}.awards` once; write summary doc; audit; `clear_cache()`. `get_public_summary`: `{published:false}` when missing.
Views: `GET /hackathons/<event_id>/peer-vote/slate`, `POST .../ballot` (`require_user`); `GET .../results`, `POST .../ballots/<propel_id>/void`, `POST .../publish` (admin); `GET .../summary` public.
Tests: own-team exclusion; unsubmitted/inactive excluded; slate capped; least-exposed first; second GET identical + exposure incremented once; `< 2` no persist; ballot matrix; voided excluded; `wilson_lower_bound(0,0)==0`, `(5,5)≈0.566`, `(1,1)≈0.207`; publish idempotent; summary gating; disabled gate.

### A8. Deadline reminders
```python
REMINDER_KINDS = {"submission"}; REMINDER_HOURS = {24, 6, 1}
def build_reminder_message(team, event, deadline_iso, hours_before) -> Optional[str]   # None when fully done; bullets for what's missing + link https://www.ohack.dev/hack/{event_id}/manageteam
def send_deadline_reminders(event_id, kind, hours_before, *, only_if_due=False, force=False, actor="cron") -> (dict, int)
def send_due_reminders_for_current_events() -> (dict, int)   # get_hackathon_list("current") × REMINDER_HOURS, only_if_due=True
```
400 bad kind/hours; 404 event; 409 `no_deadline`; idempotency key `f"{kind}_{hours_before}h"` in `reminders_sent` → 409 `already_sent` unless `force`; `only_if_due` → `{skipped:"not_due"}` unless `deadline - hours*3600 <= now < deadline`. Teams: `hackathon_event_id ==` query, skip `active == False` or no `slack_channel`. `send_slack(message, channel=slack_channel)` gated by `_notifications_disabled()` (`simulated: true` in test). Record `reminders_sent[key]` via merge; response `{success, kind, hours_before, deadline, teams_total, notified[], skipped[{team_id, reason}], simulated}`; audit; `hackathons_service.clear_cache()`.
Views: `POST /api/hackathons/<event_id>/deadlines/remind` — `@auth.optional_user` then `is_admin(auth_user) or check_api_key(request, "BACKEND_CRON_TOKEN")` (`common/utils/api_key.py`) else 403; `POST /api/hackathons/deadlines/remind-due` — API key only. Scheduling: `.github/workflows/deadline-reminders.yml` (`schedule: "7 * * * *"` + `workflow_dispatch`) → `curl -fsS -X POST "$BACKEND_URL/api/hackathons/deadlines/remind-due" -H "X-Api-Key: ${{ secrets.BACKEND_CRON_TOKEN }}"`. Requires `BACKEND_CRON_TOKEN` in Fly + GH secrets.

### A9. Judging — video only + bug fixes
See main plan WS-A task 9. No other judging changes.

### A10. Wire-up
`api/__init__.py`: import + `register_blueprint` for `api.submissions.submissions_views` and `api.peer_votes.peer_votes_views` after `broadcasts_views`. CLAUDE.md section "Project submissions + Hackers' Choice (Sep 2026)". Run: `ENVIRONMENT=test pytest api/submissions/tests api/peer_votes/tests api/github/tests api/judging/tests api/volunteers/tests test/common/utils api/messages/tests/test_hackathon_deadlines.py` + `pylint -E api/submissions api/peer_votes`.

---

## Appendix B — Team Dashboard (WS-B) — `frontend-ohack.dev`

### B0. Verified facts that shape the build
- `GET /api/team/<event>/me` **strips `users[]`** (`teams_service.py` L585–589) but keeps every other field (`mentor_*`, `github_links`, `slack_channel`, `devpost_link`, `demo_video_url`, new `project_*`). Roster therefore needs one public `GET /api/messages/team/<id>` for the active team (`usePublicTeam`). Initial load = 5 requests for a team holder, 4 without a team.
- `POST /api/messages/upload-image` is `@auth.require_user` only (admin decorator commented out, `messages_views.py` L809–811); form fields `file`, `directory`, `filename`; response `{success, url}`. Use `directory=teams/<team_id>/project`.
- `mentor_notes[] = {author_name, body, created_at, deleted_at?}`; `mentor_flags[] = {severity: needs_attention|blocked, body, created_at, resolved_at?}`.
- `TeamStatusPanel.js` and `FormStepper.js` are imported only by `manageteam.js` → deletable. `parseGithubRepo`/`normalizeRepoLink` in `ProblemStatement.js` L107–115 are private — WS-0 copied them into `src/lib/githubLinks.js`; leave ProblemStatement alone.
- GA: use `trackEvent({ action, params: { event_category: EventCategory.ENGAGEMENT, ... } })`.
- `hasApprovedTeam` (L708–711) tests `"APPROVED"`/`"PROJECT_COMPLETE"`, neither exists → re-derive as `myTeams.some(t => t.status && t.status !== "IN_REVIEW" && t.status !== "INACTIVE")`.
- findteam favorites shape: `{user_id, name, slack_user_id, github, ...}` (`findteam.js` L328–332).

### B1. File map
```
src/lib/teamDeliverables.js (+ __tests__)      deriveDeliverables(), deadlineState()
src/hooks/use-github-activity.js               fire-once IO + /activity + /issues, ONE setState
src/hooks/use-team-project.js                  draft + debounced POST autosave + submit
src/hooks/use-public-team.js                   GET /api/messages/team/<id> roster (skips if team.users present)
src/components/TeamDashboard/
  TeamDashboard.js  TeamSwitcher.js  DashboardSection.js  TeamMasthead.js  TeamStatusHero.js
  DeadlineStrip.js  DeliverablesChecklist.js  ProjectWriteupEditor.js  DemoVideoEditor.js
  DevPostEditor.js  CodeActivityCard.js  MentorSupportCard.js  MentorAvailabilityToggle.js
  SlackCoachCard.js  TeamRoster.js  HackersChoiceCard.js  CreateTeamFlow.js  GatingPanels.js  copy.js
src/pages/hack/[event_id]/manageteam.js        composition rewrite; logic verbatim
DELETE src/components/TeamCreation/TeamStatusPanel.js, FormStepper.js
EDIT   src/components/TeamCreation/TeamMemberManager.js  (remove console.log L27)
```
Every subcomponent is a module-scope function component. Everything under `TeamDashboard/` renders inside `RefinedRoot` (use `.ohx-*` directly) except Dialog/Snackbar (Portal) content, which uses `var(--brand, #1B3A6B)` fallbacks. Uses WS-0: `slackLinks.js`, `githubLinks.js`, `teamDashboardApi.js`, `use-countdown.js`, `PeerVoteCTA.js`, `projectMeta.js`.

### B2. Ordered steps
1. `teamDeliverables.js` + tests (`npm test -- teamDeliverables`). 2. Hooks. 3. Leaf cards in this order: `DeadlineStrip`, `DeliverablesChecklist`, `DemoVideoEditor`, `DevPostEditor`, `ProjectWriteupEditor`, `CodeActivityCard`, `MentorSupportCard` + `MentorAvailabilityToggle`, `SlackCoachCard`, `TeamRoster`, `HackersChoiceCard`, `TeamStatusHero`, `TeamMasthead`. 4. `TeamDashboard` + `TeamSwitcher`. 5. `CreateTeamFlow` + `GatingPanels` (JSX out of the page; handlers stay). 6. Rewrite `manageteam.js`. 7. Deletions + console.log + drop `RefinedFonts` import. 8. Verification, then CLAUDE.md.

### B3. `manageteam.js` restructure
**Keep verbatim:** `ManageTeam` default export + `RequiredAuthProvider`/`RedirectToLogin` + `postLoginRedirectUrl` SSR guard; all `useState`s; `fetchMyTeams`, `fetchHackerApplication`, `fetchHackathonEvent`, `fetchNonprofitDetails`, `fetchActiveSlackUsers` and their lazy gates (`slackFetchedRef`/`nonprofitFetchedRef`, `activeStep === 2 || showNewTeamForm`); `formError`/`teamsError` split; `filterNonprofits`/`handleSearchChange`/`clearSearch`/`toggleNonprofitSelection`/`handleDragEnd`/`handleAddTeamMember`/`handleRemoveTeamMember`; `debounce`, `validateGithubUsername`, `validateSlackChannel`; `validateForm`/`validateCurrentStep`/`handleSubmit`/`handleNext`/`handleBack`/`isNextDisabled`/`getStepContent`; `handleTeamUpdated`; post-submit `fetchMyTeams()` + `#team-hub` scroll; `noindex` `<Head>`; `SurveyCTA`.
**Change:** store the whole `GET /api/users/profile` payload as `profile` (keep `setGithubUsername(data.github)`; do not add `useProfileApi`); mount effect reading `sessionStorage["team_members"]` → `setTeamMembers(arr.filter(u => u.slack_user_id).map(u => ({ id: u.slack_user_id, name: u.name, real_name: u.name })))`, `setShowNewTeamForm(true)` when a team exists, then remove the key (try/catch, `typeof window` guard); fix `hasApprovedTeam`; `activeTeamId` state with `activeTeam = myTeams?.find(t => t.id === activeTeamId) || myTeams?.[0]` (live derivation); GA `team_dashboard_view` once (ref guard).
Render tree inside `<RefinedRoot><Box className="ohx-wrap" sx={{ pt: "clamp(88px, 9vh, 108px)", pb: 8 }}>`: `TeamMasthead` → `SurveyCTA` → `<div id="team-hub">` (skeleton `minHeight 320` while loading; `teamsError` → calm `.ohx-card` with Retry; else `TeamSwitcher` (if >1) + `TeamDashboard`) → gating (`isLoadingApplication` spinner card 160 → `NoApplicationPanel` → `AwaitingConfirmationPanel` → confirmed: `!teamCreationEnabled` notice; existing team → find-teammates row + ghost "Create another team" disclosure wrapping `CreateTeamFlow`; no team → find-teammates row + `CreateTeamFlow` inline).
`CreateTeamFlow` props `{ steps, activeStep, loading, formError, isNextDisabled, handleNext, handleBack, handleSubmit, children }` (`children = getStepContent(activeStep)`); restyle only: `refinedStepperSx` (+ mobile variant under `sm`), `primaryButtonSx`/`ghostButtonSx`, indeterminate `LinearProgress` with the existing honest copy, `errorAlertSx`; wrap in `<ThemeProvider theme={refinedFormTheme}>`.

### B4. Libs and hooks
`teamDeliverables.js`:
```js
export function deadlineState({ deadlines, endDate, timezone, submissionStatus, submittedAt, now = Date.now() })
// → { kind: 'submitted'|'open'|'late_open'|'closed'|'event_ends'|'none', target: Date|null, label, urgent }
//   target = deadlines.submission ?? end-of-day(endDate, event tz); urgent = open && target-now < 6h; late_open when now >= submission && late_submission_until && now < late_submission_until
export function deriveDeliverables({ team, activity, deadlines, slackConfirmed, now })
// → { items:[{ key, label, state:'done'|'todo'|'locked'|'optional', href:'#id', hint }], done, total, canSubmit, submitBlockedReason }
```
Rules: `slack` done ⇔ `slackConfirmed` (localStorage `ohx.team.<id>.slackConfirmed`); `code` done ⇔ `activity?.commits?.total_recent > 0` (`locked` with hint "Repository not yet created" when no repo links); `story` done ⇔ `project_tagline && project_story`; `video` done ⇔ `demo_video_url`; `submit` done ⇔ `project_submitted_at`, else `locked` when `!story || !video` (reason "Add your story and demo video first"), else `todo`; `devpost` always `optional`/`done`; `completion` only when `isWinningStatus(status) || COMPLETION_VISIBLE_STATUSES.has(status)`; `total` excludes optional; `canSubmit = story && video && !submitted && kind ∈ {open, late_open}`. Tests cover each rule, string/object `github_links`, late window, event_ends fallback, urgent boundary, submitted overrides closed.
`use-github-activity.js`: ref on the card; `IntersectionObserver({ rootMargin: "200px" })` fires once (`requestedRef` Set keyed by normalized link); `Promise.all` over repos of `[fetchGithubActivity, fetchGithubIssues]`; ONE `setState({ byRepo, status: 'ready'|'unavailable' })`; 404 from `/activity` → `unavailable`.
`use-team-project.js`: `draft` seeded from `team.project_*`; `setField`; `saveState {status:'idle'|'saving'|'saved'|'error'|'closed'|'unavailable', lastSavedAt, error}`; 1.5s debounced `POST /api/team/<id>/project` of changed keys only; success → commit + `onTeamUpdated(team.id, savedTeam)`; 409 → `closed` (stop autosave, keep `deadline`/`late_until`); 404 → `unavailable`; `submit()` flushes pending save then POSTs `/project/submit`; re-seed only when `team.id` changes.
`use-public-team.js`: return `team.users` when present, else fetch once per team id; refetch on `visibilitychange`; merge `mentor_*` from the payload via `onTeamUpdated`.

### B5. Component specs (props → responsibility)
- `DashboardSection({ id, eyebrow, title, action, children })` — `<section id sx={{ scrollMarginTop: 96, mb: {xs:4, md:5} }}>`, eyebrow + `<h2 class="ohx-display">` (`clamp(1.25rem,2.2vw,1.6rem)`) + optional right action, body `.ohx-card` `p {xs:2, md:3}`. IDs: `deliverables, project, demo, devpost, code, mentors, slack, roster`.
- `TeamMasthead({ eventId, eventTitle, team, nonprofitName, hasTeam })` — `.ohx-link` "← {eventTitle}", `<h1 class="ohx-display">` "Your team" / "Create a team"; Fraunces team name as `<p>`; meta `.ohx-tag`s (`statusLabel()` with 🏆 via `getWinningStatus`, skip `INACTIVE`; nonprofit name if resolved; `awards[]` as `.ohx-tag--accent`); `.ohx-link` "View public project page →"; faint hint "Public portfolio — recruiters and judges see this page."
- `TeamStatusHero({ team })` — only `IN_REVIEW`: `--surface-2` card, grid `1fr 220px` (stack on mobile): "We're reviewing your team" / "Review usually takes 10–20 minutes. We'll ping you in Slack when your channel and repo are ready. Nothing else to do here yet." + one `<video autoPlay loop muted playsInline preload="metadata" width=220 height=220>` picked with `useState(() => random)` from the 8-file list.
- `DeadlineStrip({ deadlines, event, team })` — `deadlineState()` + `useCountdown(target)`; `.ohx-card` with `borderLeft: 3px solid var(--brand)` (terracotta when urgent), `minHeight 64`, `position: sticky; top: 72px; zIndex: 2` on `md+`; label / Fraunces tabular digits (`minWidth: 9ch`) / `formatDualTimezone(target, getEventTimezone(event))`; submitted → "Submitted ✓ {relativeTime}" + "late" tag; GA `team_deadline_strip_view` once; visually-hidden `aria-live="polite"` span updated ≤ once/min.
- `DeliverablesChecklist({ deliverables, onSlackConfirmChange, onSubmit, submitState })` — header "What your team owes" + "N of M done"; navy `LinearProgress` (h 6); `<ol>` rows with ✓ / → / lock, label, hint, right-aligned `.ohx-link` action (anchor scroll); Slack row: "Open #channel" + checkbox "Everyone's in"; Submit row is the ONE `.ohx-btn--primary` "Submit project" (disabled with `submitBlockedReason`); after submit → "Submitted ✓ · you can keep editing until the deadline"; DevPost row "Optional"; completion row → `/completion`.
- `ProjectWriteupEditor({ project, teamId, accessToken, deadline })` — tagline `TextField` (`refinedFieldSx`, maxLength 140, `${len}/140`); `MDEditor` dynamic (`ssr:false`, loading `<Box minHeight={360}/>`), `data-color-mode="light"`, `preview="edit"` + ghost toggle to `"live"`, `height={360}`; "Insert section prompts" appends missing `##` headings; built-with `Autocomplete freeSolo multiple` (`refinedChipSx`, cap 25); links rows (label + url, cap 10, `https?://`); thumbnail `<input type=file accept=image/*>` → `uploadTeamImage` → `project_thumbnail_url` (preview `<img width=160 height=90>`) + URL paste field; footer save indicator ("Saving…" / "Saved · Ns ago" / "Couldn't save — we'll retry on your next edit") + **Submit project** → Dialog "Submit your project?" / "You can keep editing until the deadline. Submitting tells judges and mentors it's ready." / Submit · Not yet → `project.submit()` → `react-confetti` (ssr:false, `recycle={false}`, 250 pieces, 4s; skipped under reduced motion) + GA. 409 → `warningAlertSx` "Submissions closed {time}. {late_until ? `Late submissions are open until …` : 'Reach out in #ask-a-mentor if something went wrong.'}". 404 → "Project writeups aren't available for this event yet."
- `DemoVideoEditor` / `DevPostEditor` — port `isValidDemoVideoUrl` / `isValidDevPostUrl` + submit handlers verbatim from `TeamStatusPanel`; `LiteVideoThumbnail` 320×180 frame always reserved; helper "4 minutes or less. Show the problem, the demo, and who it helps."; DevPost eyebrow "Optional", lead "Some teams like an extra portfolio link. Your public project page already covers the essentials."; notifications via one page-level `Snackbar` (`onNotify`).
- `CodeActivityCard({ team, event })` — `repoEntriesFromTeam(team)`; none → "Repository not yet created — it's made when your team is approved." (+ `event.github_org` link); per repo: link, "Last commit {relativeTime} — “{message}” by {author}", `Stat` row (commits 24h, contributors, open issues, open PRs), contributor avatars `<img width=28 height=28 loading=lazy>`, top 3 open issue titles; empty repo → coaching + `<details><summary>git quickstart</summary>` (4 commands); `unavailable` → links only; `minHeight 140`.
- `MentorSupportCard({ team, eventId, accessToken, onTeamUpdated })` — `MentorAvailabilityToggle` on top; "What mentors check with you" (6 rows, ✓ when `coverageItemCovered`); "Judging readiness" (5 rows: dot from `SCORE_DOT_COLORS` exported from `TeamMentorSummaryCard`, label, blurb, `SCORE_META[consensus].label` or "Not rated yet"; link `/hackathon-judging-criteria`); "Open flags" (unresolved, `.ohx-tag--accent` Blocked/Needs attention + body); "Notes from mentors" (latest 5 non-deleted, reverse-chronological, "author · relativeTime" + body; empty: "No notes yet — mentors usually drop in within the first few hours."); footer "Full mentor view →".
- `MentorAvailabilityToggle({ team, accessToken, onTeamUpdated })` — `role="radiogroup"` `aria-label="Mentor availability"`, two `role="radio"` halves (`minHeight 56`), `aria-checked`, arrow keys; selected "Open to mentors" `#2f6e50` on white, "Heads-down" `#b04a36`; helper: open → "Mentors may drop into your channel to check in."; heads-down → "Mentors will hold off unless you ask in #ask-a-mentor."; optimistic, revert on failure; 404 → hide.
- `SlackCoachCard({ team, eventId })` — "Open #{slack_channel}" via `slackChannelUrl`; "Your ohack.dev login doesn't create a Slack account — join via /signup first." (`useEnv().slackSignupUrl`); tips with one action each: copy standup template (`**Yesterday:** \n**Today:** \n**Blockers:** `), "Ask in #ask-a-mentor" (`KEY_CHANNELS.askAMentor`), "Pin your repo + demo link" (copies both URLs), "Reply in threads"; footer "Employers can read your channel later — it's part of your public portfolio."; GA `team_slack_tip_click{tip}`.
- `TeamRoster({ users, loading, ownId, eventId, teamId, teamFindingEnabled })` — `<img width=40 height=40>` avatars (initial fallback), names → `/profile/<id>`, "You" tag when `user.id === ownId`; skeleton row 56; ghost "Invite a teammate" (copies `${origin}/hack/<e>/team/<id>`, hint "Send them this link — they click Join on your public team page"); "Find teammates →".
- `HackersChoiceCard({ eventId, deadlines, constraints })` — render `PeerVoteCTA variant="dashboard"` (WS-0) — it owns the window/enabled gate; optionally pre-check `fetchHackerVolunteerMe` to hide for non-hackers.
- `TeamSwitcher({ teams, activeId, onChange })` — MUI `Tabs` (navy indicator, `textTransform: none`), label = name + tiny status tag, `aria-label="Your teams"`; compact line "You're on more than one team — pick the one you're working on."
- `GatingPanels.js` — current copy from `manageteam.js` L798–880 moved into `NoApplicationPanel` / `AwaitingConfirmationPanel`, emoji dropped, Slack join → `useEnv().slackSignupUrl`; neutral framing kept; don't change wording in `findteam.js` unless you change it here.
`TeamDashboard` order: `TeamStatusHero → DeadlineStrip → DeliverablesChecklist → (HackersChoiceCard) → ProjectWriteupEditor → DemoVideoEditor → CodeActivityCard → MentorSupportCard → SlackCoachCard → TeamRoster → DevPostEditor`. Editors always open. On `IN_REVIEW`: hero + strip + Slack + roster only.

### B6. Copy (`copy.js`)
h1 "Your team" / "Create a team" · Deliverables title "What your team owes", lead "Five things, roughly in order. Everything here is public when you're done." · rows: "Join your Slack channel" · "Push code to your repo" · "Write your project story" · "Add a demo video" · "Submit your project" · "Link your DevPost project (optional)" · "Finish the Definition of Done" · story lead "Judges read this before they watch anything. Plain language beats buzzwords." · code empty "Push your first commit — judges and recruiters look here first." · mentors title "Mentor support" · Slack title "Where your team talks" · roster title "Who's on the team" · deadline fallback "Event ends".

### B7. Styling, CWV, a11y, GA
Only `.ohx-*` + tokens; MUI via `refinedFormTheme` + the `refinedStyles.js` sx exports; one `.ohx-btn--primary` per section; control rows `py 2.25 / minHeight 64`; fonts via `var(--display)`/`FONT_BODY` only. Reservations: hub 320, strip 64 + 9ch digits, editor 360, video 320×180, code 140, roster 56, waiting video 220×220; all `<img>` have width/height. Requests on load: hackathon, team/me, hacker/application, users/profile (+ messages/team/<id>); GitHub only on IO; Slack users + nonprofit fan-out only when the create form opens. A11y: one `<h1>`, `<section>`+`<h2>`, `<ol>` checklist with text state, radiogroup toggle, throttled live region, "Copied" `aria-live`. GA listed in the main plan WS-B.

### B8. Verification
Main plan Part 6 item 2, plus: `npm test -- teamDeliverables`; `grep -rn "TeamStatusPanel\|FormStepper" src` → none; Lighthouse mobile CLS < 0.05; no console errors on 409/404 paths.

---

## Appendix C — Project page, gallery, results, judge video, mentor tag (WS-C), Vote page (WS-D), Admin (WS-E)

### C0. Shared modules (built in WS-0; signatures)
| File | Exports |
|---|---|
| `src/components/Teams/projectMeta.js` | `getSubmissionStatus(team)` → `'submitted'|'late'|'draft'|null` (**null when neither `project_submission_status` nor `project_submitted_at` exists**); `submissionLabel(team, tz)` → `{tag, line}`; `formatDeadline(iso, tz)` → `"Sat 2:58 PM MST"`; `projectThumbUrl(team)` (cdn thumb → YouTube `hqdefault` from `demo_video_url` → null); `buildTeamOgImage(team)` → `{image, card}`; `buildTeamDescription(team, {nonprofitName, eventName, memberCount})`; `buildProjectJsonLd(team, {canonicalUrl, eventName, eventUrl})` → `SoftwareSourceCode` node or null; `firstRepoUrl(team)`; `isProjectStoryMissing(team)` |
| `src/components/Teams/ProjectStoryMarkdown.js` | default `({ markdown, demoteBy = 1, style })` — `dynamic(() => import('react-markdown'), { ssr: true })`; headings demoted by `demoteBy`; `img` lazy + `max-width:100%`; `a` `target=_blank rel=noopener noreferrer`; no `rehype-raw` |
| `src/hooks/use-countdown.js` | `useCountdown(targetIso, { intervalMs = 1000 })` → `{ mounted, ms, days, hours, minutes, seconds, done }` (`mounted:false` until after first effect) |
| `src/components/PeerVote/peerVoteState.js` | `deriveVoteWindow(deadlines, constraints, nowMs)` → `'disabled'|'unscheduled'|'upcoming'|'open'|'closed'` (mirrors backend defaults); `togglePick(picks, id, max)`; `canSubmit(picks, max)`; `slateLinks(item, eventId)` |
| `src/components/PeerVote/PeerVoteCTA.js` | default `({ eventId, deadlines, constraints, variant = 'event', style })` — SurveyCTA clone (var-fallback inline styles, mount gate, `role="complementary"`); null unless open, or upcoming within 24h |
Note: `next.config.js` `remotePatterns` allows `cdn.ohack.dev` but **not** `img.youtube.com` — `next/image` only for CDN thumbnails; YouTube posters are plain `<img width height loading="lazy">`.

### C1. Team page `src/pages/hack/[event_id]/team/[team_id]/index.js`
1. Delete local `COMPLETION_VISIBLE_STATUSES`/`SCROLL_OFFSET`/`OG_IMAGE`/`statusLabel` (L40–55); import from `../../../../../components/Teams/teamPageData`. Remove `RefinedFonts`.
2. New module-scope `src/components/Teams/TeamProjectSection.js` — `({ team, eventId, eventTimezone, isOnTeam, membershipChecked })` rendered inside the existing `SectionBlock` (`id="project"`, title "Project"): tagline `.ohx-lead`; thumbnail (`next/image` only for `cdn.ohack.dev`, `width 960 height 540`, `sizes="(max-width: 900px) 100vw, 760px"`; otherwise `<img width=960 height=540 loading=lazy decoding=async>`) inside an `aspectRatio: '16 / 9'` `--surface-2` box; `<ProjectStoryMarkdown markdown demoteBy={1} />`; built-with `.ohx-tag`s; project links as `.ohx-card ohx-card--hover` tiles (`target=_blank rel=noopener noreferrer`); submission line (muted 0.9rem): public "Submitted Sat 2:58 PM MST" / "Submitted late …" / quiet `.ohx-tag` "In progress"; members: "Draft — not yet submitted." + `.ohx-link` "Edit on your dashboard →" (`/hack/${eventId}/manageteam#project`); nothing when status is null. Section renders when any project data exists; members with none see a calm `--surface-2` card "Tell the story of what you built." + "Add your project story →".
3. `sections.unshift({ id: "project", name: "Project" })` before `mentor-support`; render first. TOC/IO code untouched.
4. Masthead: `awards[]` loop already renders "Hackers' Choice" — add `aria-label="Award"`; add a submission `.ohx-tag` after the member count when status non-null.
5. Member nudge (L497–523): `missingStory = isProjectStoryMissing(team)`, `missingDemo = !team.demo_video_url`, `notSubmitted = getSubmissionStatus(team) === 'draft'`; copy "Your project page is missing {a story, a demo video}." + (notSubmitted ? " It isn't submitted yet." : "") + "Finish it on your dashboard →" (`manageteam#project`). DevPost no longer nagged.
6. `<Head>`: `buildTeamDescription` → description; `buildTeamOgImage` → `og:image`/`twitter:image`/`twitter:card`; `<script type="application/ld+json">` with `buildProjectJsonLd(team, { canonicalUrl, eventName, eventUrl })` when non-null (`SoftwareSourceCode`: name, description, url, codeRepository, image?, dateModified?, isPartOf Event).
7. Links section: DevPost tile stays (only when present).

### C2. Gallery `src/components/Hackathon/TeamList.js`
1. Module-scope `ProjectMedia({ team, onPlayVideo })` above `TeamCard`: 16:9 box (`aspectRatio`, `background: var(--surface-2,#F4F1E9)`, radius 8, `overflow hidden`, `mb 1.5`): `project_thumbnail_url` → `<img width=320 height=180 loading=lazy decoding=async objectFit cover>` (wrapped in a play `<button aria-label="Play demo">` when `demo_video_url`) → else `demo_video_url` → existing `LiteVideoThumbnail onClick={() => onPlayVideo(url, team.name)} width=480 height=270` → else Fraunces initial placeholder (`var(--brand)`, 56px). Remove the old `LiteVideoThumbnail` block at L1335; media sits directly under the inactive banner.
2. Header additions after name + status chip: tagline `body2` muted 2-line clamp; built-with first 3 quiet tags + `+N`; submission tag via `getSubmissionStatus` ("Submitted" navy outline / "Submitted late" muted / "In progress" faint; omitted when null). Everything below (MentorSupportSummary, Slack, nonprofit, GitHub, created, members, join/leave, `onVisible` lazy avatars) unchanged.
3. DevPost block (L1343–1372): present → quiet 0.9rem muted "DevPost ↗"; absent → if `isUserInTeam` a `NextLink` "Add your project story →" to `/hack/${event_id}/manageteam#project` (use the `event_id` prop), else nothing.
4. `submittedOnly` state + `visibleTeams = useMemo` (stable sort: submitted/late first, preserving the caller's active-first order; filter when toggled); quiet `<button aria-pressed>` "Submitted only ({n})" above the grid, rendered only when `n > 0`; empty state "No submitted projects yet." No new fetches.

### C3. Results + funnel + constants
`HackathonResults.js` (also rendered on `/hack/[event_id]/results` outside a `RefinedRoot` → `var(--x, #hex)` fallbacks): winner card gets tagline (`body2` muted 2-line clamp) and `awards[]` quiet pills (`border 1px solid var(--line,#E7E1D4)`, `background var(--accent-soft,#FBE9E2)`, 🏆 prefix; guard non-array); DevPost chip only when present. New prop `peerVoteEnabled`; module-scope `HackersChoiceCard({ eventId, teams, peerVoteEnabled })` → nothing when disabled; else `minHeight 148` slot after the winners grid, client fetch `GET ${API}/api/hackathons/${eventId}/peer-vote/summary` (404/err → "Hackers' Choice — announced at the awards ceremony."; published → eyebrow "Hackers' Choice", team name link, tagline, "{ballots} peer ballots · chosen by fellow hackers"). Event page L798 block and `results.js` pass `peerVoteEnabled={!!event.constraints?.peer_vote_enabled}`.
`HackathonFunnel.js` L181/205/213 sublabels → "People who registered for the event", "People on a project (any state)", "People on a submitted project".
`TeamsShowcaseSection.jsx` L30–34 → `import { WINNING_STATUSES } from "../../../constants/teamStatus"`; `const SHOWCASE_TROPHY_STATUSES = new Set([...WINNING_STATUSES.map(s => s.value), "DEPLOYED", "NONPROFIT_SIGNOFF"])`.

### C4. Judge video (no process change)
`src/pages/judge/[event_id]/team/[team_id].js` (link row L717–760): when `teamData.demo_video_url || teamData.video_url`, render `VideoDisplay` (dynamic ssr:false) inside a `paddingBottom:'56.25%'` box under the links + a "Watch demo" button. `src/pages/judge/[event_id].js` L264–269: video button reads `demo_video_url || video_url`. Nothing else in judge/admin judging files changes.

### C5. Heads-down signal on mentor surfaces
`isHeadsDown = team.mentor_help_wanted === false`. `src/components/Mentor/MentorTeamsTable.js`: add an "Availability" column next to Coverage (module-scope `AvailabilityCell` → `.ohx-tag` "Open" / `.ohx-tag--accent` "Heads-down", tooltip "Team asked for heads-down time — check Slack before dropping in"; also in the mobile card). `MentorTeamPanel.js`: header tag with the same text when heads-down. `TeamMentorSummaryCard.js` and `TeamList.js` `MentorSupportSummary`: one quiet muted line "Heads-down — team asked mentors to hold off". No sorting/behaviour changes.

### C6. Event page `src/pages/hack/[event_id].js`
Static import `PeerVoteCTA`; render after `<SurveyCTA …/>` (L763): `<PeerVoteCTA eventId={event.event_id || event_id} deadlines={event.deadlines} constraints={event.constraints} />`. Pass `peerVoteEnabled` to `HackathonResults`.

### D. Vote page `/hack/[event_id]/vote` (WS-D)
Files: `src/pages/hack/[event_id]/vote.js`, `src/components/PeerVote/{PeerVotePage, PeerVoteSlateCard}.js`.
Shell (~25 lines): `<Head>` title "Hackers' Choice vote | Opportunity Hack" + `<meta name="robots" content="noindex, nofollow" />`; `RequiredAuthProvider authUrl={NEXT_PUBLIC_REACT_APP_AUTH_URL} displayIfLoggedOut={<RedirectToLogin postLoginRedirectUrl={…} />}` (manageteam L1050–1059 pattern incl. the SSR `window` guard); `dynamic(() => import('../../../components/PeerVote/PeerVotePage'), { ssr:false, loading: () => <Shell><Skeleton height={320}/></Shell> })`. `next-sitemap.config.js`: add `"/hack/[event_id]/vote"` to `exclude`.
`PeerVotePage`: `Shell` + `TeamBreadcrumbs items=[{name: event title, href}] current="Hackers' Choice vote"`; fetch `GET ${API}/api/hackathons/${event_id}/peer-vote/slate` (Bearer from `useAuthInfo`) on mount + `visibilitychange` when upcoming; GA `peer_vote_view{event_id, status}`. Masthead: `Eyebrow` event title; `<h1 className="ohx-display">Hackers' Choice</h1>`; `.ohx-lead` exactly: "You'll see {slate_size} projects picked for you. Pick up to {max_picks} you'd be proud to have built. Every project gets seen roughly equally; you can't see or pick your own." (fallback 5/2 while loading). States (module-scope `.ohx-card`s): `disabled`/404 → "Voting isn't set up for this event yet."; `not_eligible` → "This vote is for registered hackers." / "Only hackers who registered for {event} can vote. If you're on a team and see this, ask an organizer in Slack." + ghost "Back to event"; `not_enough_submissions` → "Not enough submitted projects yet — check back later."; `upcoming` → "Voting opens {formatDeadline(opens_at, tz)}" + `useCountdown` "Opens in 1d 4h 12m" (`aria-live="polite"`); `open` → grid `repeat(auto-fill, minmax(280px,1fr))` gap 20 of `PeerVoteSlateCard` + sticky bottom bar (`position: sticky; bottom: 0; background: var(--paper); borderTop: 1px solid var(--line)`) "{n} of {max_picks} picked" + `.ohx-btn--primary` "Submit picks" (disabled until n ≥ 1) + faint "Closes {formatDeadline(closes_at)}"; confirm `Dialog` "Submit your picks?" listing names + "You can change them until voting closes."; `POST …/ballot {picks}` → `voted` + GA `peer_vote_submitted{value: picks.length}` + `react-confetti` (ssr:false, `recycle={false}`, 220 pieces, skipped under reduced motion) + "Thanks — your picks are in." / "Results are announced at the awards ceremony." + ghost "Share the event" (`navigator.share` else copy `https://www.ohack.dev/hack/${event_id}`) + "Back to event"; 409 → refetch slate + inline "Voting is closed or one of your picks changed — we refreshed your slate."; `voted` → "Your picks" (names, taglines) + ghost "Change picks" while open; `closed` → "Voting has closed." / "Winners are announced at the awards ceremony — check the event page afterwards." + link `/hack/${event_id}#results`. No tallies anywhere.
`PeerVoteSlateCard({ item, selected, disabled, watched, onToggle, onWatched, onPlayVideo, eventId })`: `<article aria-labelledby>`; media (thumbnail → `LiteVideoThumbnail` → initial placeholder; 16:9 reserved); Fraunces name; tagline 3-line clamp; built-with first 4; "GitHub ↗" (`github_links[0]`), "Project page ↗" (`/hack/${eventId}/team/${team_id}`, new tab); `<label><input type="checkbox"> Watched</label>` (local); pick button `aria-pressed` "Pick this project" / "Picked ✓" (disabled at max with title "You've used all your picks"). ONE page-level `VideoDisplay` `Dialog` (TeamList L1989–2015 pattern). Focus rings `outline: 2px solid var(--brand)`.

### E. Admin (WS-E)
**E1 `DeadlinesSection`** — `sectionsManifest.js`: after `schedule` insert `{ slug:"deadlines", label:"Deadlines", icon: Alarm, hint:"Submission cutoff, late window, Hackers' Choice voting", group:"config" }`; `pages/admin/hackathons/[event_id].js` loader `deadlines: dynamic(() => import(".../sections/DeadlinesSection"), { ssr:false })`; `useHackathonAdmin.js`: `EXPLICIT_SAVE_SECTIONS.add("deadlines")`, `SECTION_LABELS.deadlines = "Deadlines"`, `collectKeysForSections` → `else if (s === "deadlines") { keys.add("deadlines"); keys.add("countdowns"); }`. Move `toIsoWithTimezone` + `safeParse` from `ScheduleSection.js` into `src/lib/timezoneUtils.js` (export; ScheduleSection imports). Pure `hackathon-edit/deadlinesUtils.js` + tests: `validateDeadlines(d)` → `{errors, warnings}` (`late ≥ submission`; `voting_closes > voting_opens`; warning when `voting_opens < submission`), `upsertCountdownByName(countdowns, {name, time, description})` (case-insensitive match → replace time/description keep id; else append), `hoursBeforeLabel(h)`. Section body inside `SectionContainer title="Deadlines" description="Submission cutoff and Hackers' Choice window. Times are saved in the event timezone ({tz}). Explicit save." dirty onSave onDiscard saveState`: four `DateTimePicker`s (`LocalizationProvider AdapterDateFns`) bound to `hackathon.deadlines?.*` → `setField("deadlines", {...cur, [k]: toIsoWithTimezone(d, tz)})` + `markSectionDirty("deadlines", true)`, helper text `formatDualTimezone`, inline error Alerts, Save disabled on errors; "Add to countdown timeline" → `setField("countdowns", upsertCountdownByName(..., { name:"Submissions close", time: deadlines.submission, description:"Final project submissions are due." }))` + dirty + snack; Hackers' Choice card: `Switch` `peer_vote_enabled`, numbers `peer_vote_slate_size` (3–10), `peer_vote_max_picks` (1..slate−1), `Switch` `peer_vote_requires_submission` via `admin.setConstraint(...)` (autosave); "Send reminder now" 24h/6h/1h → `axios.post(${API}/api/hackathons/${event_id}/deadlines/remind, { kind:'submission', hours_before }, Bearer + X-Org-Id)` → snack; disabled while dirty or no deadline; GA `admin_deadline_reminder_sent{event_id, hours_before}` (`EventCategory.ADMIN`).
**E2 `TeamManagement.js`** — import `getSubmissionStatus`, `formatDeadline`; module-scope `SubmissionChip({ team, tz })` (submitted → success outlined "Submitted"; late → warning "Late"; draft → default "Draft"; null → "—"; tooltip submitted_at); header (L2458–2463) insert "Submission" + "Story" after "Demo Video"; filter chips (L2342–2349) append `missing_story`, `not_submitted`, `late`, move `missing_devpost` last; switch (L423–437) cases; edit Dialog Team Details (near L1675): tagline `TextField` (140 + counter), `Select` "Submission status" (draft/submitted/late; helper "Override only — e.g. accept a late submission."), story read-only `ProjectStoryMarkdown` preview (`maxHeight 240`, scroll) + "Open public page ↗" + "Edit story" toggle revealing `TextField multiline minRows=8`; `handleSaveTeam` already spreads `teamData`; pure `src/components/admin/teamSubmissionsCsv.js` `buildSubmissionsCsv(teams, nonprofitMap)` (RFC-4180; columns `name, tagline, submission_status, submitted_at, demo_video_url, github_url, devpost_link, slack_channel, members, nonprofit`) + test; "Export submissions CSV" button → Blob download `submissions-${selectedHackathon}.csv`; GA `admin_submissions_csv_export`.
**E3 `PeerVoteResults.js`** — `JudgingSection.js`: destructure `{ admin, accessToken, orgId, onSnack }`; `TAB_NAMES = ["round1","round2","results","peer-vote"]`; 4th Tab "Hackers' Choice" → `<PeerVoteResults eventId accessToken orgId onSnack votingCloses={admin.hackathon?.deadlines?.voting_closes} enabled={!!admin.hackathon?.constraints?.peer_vote_enabled} />` (dynamic ssr:false). Component: `Promise.all([GET …/results, GET …/summary])` (Bearer + X-Org-Id; summary 404 → null) → ONE setState; header "{ballots} ballots · ~{eligible_estimate} eligible ({pct}%)" + Refresh; `!enabled` info Alert "Hackers' Choice is off for this event — enable it under Deadlines."; table Rank · Team (public link) · Shown · Approvals · Rate · Wilson LB · `LinearProgress` bar (navy); `shown < 3` rows get a warning icon + tooltip "Seen by fewer than 3 voters — rank unreliable." and a top-level warning count; void ballot (`TextField` propel id + confirm Dialog → `POST …/void` → refetch + snack); Publish (disabled until `Date.now() > Date.parse(votingCloses)` and not published; confirm "Publish Hackers' Choice? This appends the award to {rank-1 team} and shows it publicly." → `POST …/publish` → refetch; banner "Published {date} · Hackers' Choice: {team}"); GA `admin_peer_vote_publish`, `admin_peer_vote_void_ballot`. Permission: host page `/admin/hackathons/[event_id]` already gates `volunteer.admin` (matches backend).

### Freshness notes (for all frontend workstreams)
Team page: ISR 60s + client refetch on mount → edits appear immediately client-side. Event gallery + `PeerVoteCTA`: ISR 60s **plus** backend `get_single_hackathon_event` 10-min TTL — the backend project-save/publish paths call `hackathons_service.clear_cache()` so the residual lag is the 60s ISR window.

### Docs to write (WS-F)
CLAUDE.md: "Project pages & gallery (Sep 2026)" (fields; `projectMeta.js` single source for submission status/OG/JSON-LD; `ProjectStoryMarkdown` demotion; `getSubmissionStatus` null-for-legacy; `next/image` only for `cdn.ohack.dev`; gallery sort/filter; DevPost optional), "Hackers' Choice peer vote" (routes, states, `PeerVoteCTA` shared with the dashboard, no tallies to voters, GA names), "Hackathon deadlines section" (explicit-save keys `deadlines`+`countdowns`; peer-vote constraints autosave; reminder endpoint), team-page invariants (9) Project first (10) OG via `projectMeta`, Admin Teams + Hackathon Admin Edit lists, Funnel sublabels, `mentor_help_wanted` absent ⇒ true, the replaced manageteam paragraph + invariants, Part 9 documented-only bugs as gotchas. `docs/refined-design-system.md` rollout: `/hack/[event_id]/vote`, team Project section, gallery media. `ga-events-reference.md`: all new events.
